pragma solidity ^0.4.19;

address x = 0x12356789;
address MyAddress = this;

// "balance" return the balance
// "send" return a boolean value
if (x.balace < 10 && myBalance >= 10) x.send(10);

// if "send" fails o run out of gas, returns "false"